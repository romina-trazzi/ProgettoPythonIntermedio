// Questo file può contenere script JavaScript globali o funzioni di utilità.
// Per ora, lo lasceremo vuoto o aggiungeremo un semplice log per dimostrazione.

console.log("Script.js caricato con successo!");

// Esempio di funzione JavaScript (può essere espansa in seguito)
function showAlert(message) {
    alert(message);
}